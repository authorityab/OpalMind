MATOMO_BASE_URL=
MATOMO_TOKEN=
# Optional. Provide only when a default Matomo site should be assumed.
MATOMO_DEFAULT_SITE_ID=
# Optional. Provide a JSON or YAML file with site name → site ID mappings.
MATOMO_SITE_MAP_PATH=./config/site-mapping.json
# Optional. Inline JSON override for the site map (takes precedence over MATOMO_SITE_MAP_PATH).
MATOMO_SITE_MAP_JSON=
# Generate with `openssl rand -hex 32` and inject via your secret manager.
OPAL_BEARER_TOKEN=
PORT=3000
