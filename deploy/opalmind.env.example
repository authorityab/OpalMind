MATOMO_BASE_URL=
MATOMO_TOKEN=
# Optional. Provide only when a default Matomo site should be assumed.
MATOMO_DEFAULT_SITE_ID=
# Generate with `openssl rand -hex 32` and inject via your secret manager.
OPAL_BEARER_TOKEN=
PORT=3000
