version: '3.9'

services:
  matokit-api:
    image: ghcr.io/puttrix/matokit-api:latest
    env_file:
      - matokit.env
    environment:
      - NODE_ENV=production
    ports:
      - '4000:4000'
    deploy:
      restart_policy:
        condition: unless-stopped
