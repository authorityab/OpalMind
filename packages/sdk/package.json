{
  "name": "@opalmind/sdk",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint 'src/**/*.ts' 'test/**/*.ts'",
    "test": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opalmind/logger": "file:../logger",
    "ioredis": "^5.8.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/ioredis": "^4.28.10"
  }
}
